# dsh-grok-xsearch

Publishable DSH plugin (`@goodandready/dsh-grok-xsearch`).

- Separate Grok OAuth from dsh-subscriptions (`GROK_XSEARCH_OAUTH_1`).
- No secrets in git; OAuth client id via Settings only.
- Gitea: goodandready/dsh-grok-xsearch
